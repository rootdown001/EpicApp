import { Inter, Alata } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const alata = Alata({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
  variable: "--font-alata",
});
